This app requires the SQL formatted war diary archive from Wikileaks to be present in the root of the app during the "rake install".

sudo gem update --system
sudo gem install rails -v=2.3.8
sudo rake gems:install
rake install
rake sunspot:solr:start
./script/server
